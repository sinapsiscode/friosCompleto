const express = require('express');
const router = express.Router();

// Placeholder para rutas de herramientas
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de herramientas - En desarrollo' });
});

module.exports = router;