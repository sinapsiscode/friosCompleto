const express = require('express');
const router = express.Router();

// Placeholder para rutas de repuestos
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de repuestos - En desarrollo' });
});

module.exports = router;