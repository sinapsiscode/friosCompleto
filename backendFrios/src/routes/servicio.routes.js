const express = require('express');
const router = express.Router();

// Placeholder para rutas de servicios
router.get('/', (req, res) => {
  res.json({ message: 'Rutas de servicios - En desarrollo' });
});

module.exports = router;